// import * as React from 'react';
// import styled from 'styled-components';


// Create styled navbar, and add position: sticky styling, so that it 
// remains persistently on page, try this out and check results.


export default function NavigationBar() {
    return(
      <h2>NAV</h2>
    )
}